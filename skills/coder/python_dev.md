---
name: python_dev
description: "Python development with modern best practices"
tags: [python, backend, fastapi]
---
# Python Development Skill

## Instructions
1. Use `uv` for dependency management.
2. Ensure type hints are used everywhere.
3. Follow PEP 8 via `ruff`.
4. Tests must be written in `pytest`.

## Workflow
- Check `pyproject.toml`.
- Install dependencies: `uv sync`.
- Run `pytest` before and after changes.
